# @mds-crate/theme-sdk-html

Tagged-HTML authoring SDK for package-defined MDS themes.

It converts author-controlled HTML template components into the same plain theme artifact model used by every MDS runtime.

See the [theme authoring guide](https://github.com/ziyu/mds/blob/main/docs/THEMES.md). Licensed under Apache-2.0.
