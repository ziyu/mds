---
"@mds-crate/cli": minor
---

Add the packaged local MDS Editor with `mds edit`, real file open/create/save workflows, conflict-safe atomic persistence, project and installed-package themes, a loopback security boundary, and browser end-to-end coverage.
